#include <stdio.h>
/**
 * main - prints all alphabet
 * Return: 0
 */
int main(void)
{
 char c;

      c = 'a';
      while(c <= 'z')
      {
              printf(c);
              c = c+1;
      }
      return (0);
}








}
